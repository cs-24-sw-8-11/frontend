import 'package:flutter/material.dart';

// Scaffold (base background)
const globalScaffoldBackgroundColor = Color.fromARGB(255, 55, 55, 55);

// AppBar
const globalAppBarColor = Color.fromARGB(255, 30, 30, 30);
const globalnavigatorArrowColor = Colors.white;

// Text
const globalTextColor = Colors.white;

// Button
const globalButtonBackgroundColor = Colors.white38;
const globalButtonDisabledBackgroundColor = Colors.white10;
const globalUnderlineColor = Colors.white54;

// Animation
const globalAnimationColor = Colors.white;